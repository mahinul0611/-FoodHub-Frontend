# All frontend API calls go through a same-origin proxy (see next.config.mjs)
# so that Better Auth session cookies are first-party and login persists.
NEXT_PUBLIC_API_URL=/backend-api

# The real backend URL that the proxy forwards requests to.
BACKEND_URL=https://foodhub-backend-5.onrender.com
